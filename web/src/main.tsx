import React from "react";import{createRoot}from"react-dom/client";import"./styles.css";import{ArrowUpRight,ArrowRight,Search,ShieldCheck,FileText,Landmark,MessageCircle,Menu,X,CheckCircle2}from"lucide-react";import{AnimatePresence,motion}from"framer-motion";import{useState}from"react";
const schemes=[["PM-KISAN","Income support","Understand eligibility, documents and the official route.","https://pmkisan.gov.in/"],["PMFBY","Crop insurance","Explore crop-insurance guidance and current official procedures.","https://pmfby.gov.in/"],["Kisan Credit Card","Credit support","Start with the documents and lender route relevant to agricultural credit.","#"]];
function App(){const[help,setHelp]=useState(false),[menu,setMenu]=useState(false),[query,setQuery]=useState("");const filtered=schemes.filter(s=>s.join(" ").toLowerCase().includes(query.toLowerCase()));return <div className="app">
<header><a className="logo" href="#"><span>S</span><div><b>SHAYA</b><small>COOPERATIVE ASSISTANCE</small></div></a><nav className={menu?"open":""}><a href="#services" onClick={()=>setMenu(false)}>Services</a><a href="#schemes" onClick={()=>setMenu(false)}>Schemes</a><a href="#grievance" onClick={()=>setMenu(false)}>Grievance</a></nav><button className="ask" onClick={()=>setHelp(true)}>Ask for help <ArrowUpRight size={15}/></button><button className="menu" onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button></header>
<main>
<section className="hero"><div className="hero-copy"><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{duration:.6}}><p className="eyebrow"><i/> A civic service prototype</p><h1>Cooperative help,<br/><em>without the maze.</em></h1><p className="lead">A calm place to find schemes, understand cooperative services and prepare the next step — without needing to know the department first.</p><div className="actions"><button className="primary" onClick={()=>setHelp(true)}>Tell us what happened <ArrowRight/></button><a href="#services">Explore services ↓</a></div><div className="trust"><ShieldCheck size={15}/> Source-oriented &nbsp;•&nbsp; Human-reviewed journeys</div></motion.div></div><div className="hero-art"><div className="glow g1"/><div className="glow g2"/><motion.div className="paper-card back" animate={{y:[0,-8,0],rotate:[4,3,4]}} transition={{repeat:Infinity,duration:7}}/><motion.div className="paper-card front" animate={{y:[0,-12,0],rotate:[-4,-5,-4]}} transition={{repeat:Infinity,duration:7,delay:.4}}><div className="mini-top"><span>SHAYA</span><span>01</span></div><div className="card-word">One place.<br/><b>Clear next step.</b></div><div className="rule"/><div className="mini-bottom"><span>SCHEME</span><span>HELP</span><span>GRIEVANCE</span></div></motion.div></div></section>
<section id="services" className="section"><div className="section-title"><div><span>01</span><h2>Start with the thing<br/>you need.</h2></div><p>Four practical paths.<br/>No maze of departments.</p></div><div className="service-grid">{[[Landmark,"Find a scheme","Eligibility, documents and where to continue."],[MessageCircle,"Ask for help","Explain the situation in normal words."],[FileText,"Prepare a grievance","Turn an issue into a reviewable draft."],[ShieldCheck,"Cooperative services","Understand what to ask your PACS about."]].map(([Icon,t,d],i)=><motion.button whileHover={{y:-7}} className={"service "+(i===2?"warm":i===3?"mint":"")} key={t} onClick={()=>i===1?setHelp(true):document.getElementById(i===0?"schemes":"grievance")?.scrollIntoView({behavior:"smooth"})}><small>0{i+1}</small><Icon/><h3>{t}</h3><p>{d}</p><b>Explore <ArrowRight size={14}/></b></motion.button>)}</div></section>
<section id="schemes" className="scheme-section"><div className="scheme-head"><div><span>02 / SCHEME NAVIGATOR</span><h2>Know the name or don't.<br/><em>Either way, start here.</em></h2></div><div className="search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Try: crop insurance"/></div></div><div className="scheme-grid">{filtered.map(s=><article className="scheme" key={s[0]}><span>{s[1]}</span><h3>{s[0]}</h3><p>{s[2]}</p><a href={s[3]} target="_blank" rel="noreferrer">View official route <ArrowUpRight size={14}/></a></article>)}</div></section>
<section id="grievance" className="grievance"><div><span>03 / GRIEVANCE</span><h2>Something went wrong?<br/><em>Write it down clearly.</em></h2><p>Prepare a draft locally, review it, then continue through the appropriate official channel. This demo never pretends a complaint was submitted.</p><button className="dark-btn" onClick={()=>setHelp(true)}>Prepare the next step <ArrowRight/></button></div><div className="draft"><label>DRAFT ONLY</label><div><small>ISSUE TITLE</small><strong>Application status is not updated</strong><small>WHAT HAPPENED</small><p>Explain what happened, where it happened and what help you need.</p></div><footer>Saved locally in this browser <CheckCircle2 size={14}/></footer></div></section>
<footer><div><b>SHAYA</b><span>Cooperative assistance, designed around the person.</span></div><span>Prototype • React + TypeScript</span></footer>
</main>{help&&<Help close={()=>setHelp(false)}/>}</div>}
function Help({close}:{close:()=>void}){const[text,setText]=useState(""),[answer,setAnswer]=useState("");const route=()=>{const q=text.toLowerCase();setAnswer(/crop|insurance|pmfby/.test(q)?"This sounds related to PMFBY. Start with the official crop-insurance route and verify the current procedure.":/pacs|cooperative|society/.test(q)?"Start with the Cooperative Services route and identify the PACS/local service connected to your issue.":"Try mentioning a scheme, document, PACS, application, status, or the problem itself.");};return <AnimatePresence><motion.div className="overlay" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={e=>e.currentTarget===e.target&&close()}><motion.div className="modal" initial={{y:30,scale:.97}} animate={{y:0,scale:1}}><button className="close" onClick={close}><X/></button><span>HELP DESK</span><h2>Tell us what happened.</h2><p>You don't need to know the scheme name first.</p><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Example: I applied for crop insurance but cannot see the status."/><button className="primary" onClick={route}>Find a route <ArrowRight/></button>{answer&&<div className="answer"><CheckCircle2/><div><b>Suggested next step</b><p>{answer}</p></div></div>}</motion.div></motion.div></AnimatePresence>}
createRoot(document.getElementById("root")!).render(<App/>);